# 모듈 임포트하는 방식
# 1. import로 모듈 가져오기
# 2. import as로 모듈 이름 지정하기
# => from 모듈 as 이름 
# 3. from import로 모듈의 일부만 가져오기 
# => from 모듈 import 변수
# 4. from import로 모듈의 일부를 가져온 뒤 이름 지정하기
# => from 모듈 import 변수 as 이름